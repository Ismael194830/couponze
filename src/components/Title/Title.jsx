export default function Title({title , discreption}) {
  return (
    <div className="Diteales">
      <h2 className="Title">{title}</h2>
      <p className="Discrition">{discreption}</p>
    </div>
  );
}
